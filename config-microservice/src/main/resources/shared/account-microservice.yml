# HTTP Server
server:
  port: ${PORT:8666}
spring:
  application:
    name: accounts-server

# Discovery Server Access - "magic string fallback value that provides the service URL for any client that does not express a preference (in other words, it is a useful default)."
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


